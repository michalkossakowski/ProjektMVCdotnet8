@model IEnumerable<ProjektMVCdotnet8.Entities.CategoryEntity>
@foreach (var element in Model)
{
    <li>
        @Html.ActionLink(element.CategoryName, "Index", "Categories", new { CategoryName = element.CategoryName }, new { @class = "dropdown-item" })
    </li>
}
